/**
 * Created by harinaths on 8/4/14.
 */

var config = {
    host : '0.0.0.0',
    port : 4444,
    root : __dirname

}
var HttpServer =  require('./http.js');
var app = new HttpServer(config).connect();

app.get('/', function(req, res){
    res.send("HTTP TEST OK");
})
